/* CIF PROCESS (250, 150), (150, 75) */
process Controller;
    /* CIF START (409, 100), (70, 35) */
    START;
        /* CIF output (409, 155), (70, 35) */
        output ping;
        /* CIF NEXTSTATE (409, 205), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (636, 88), (70, 35) */
    state Wait;
        /* CIF input (636, 143), (70, 35) */
        input pong;
            /* CIF NEXTSTATE (636, 198), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Controller;