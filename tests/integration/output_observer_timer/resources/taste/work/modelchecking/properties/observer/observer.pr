system observer;
    /* CIF TEXT (110, 252), (459, 140) */
    use datamodel comment 'observer.asn';
    
    signal trigger_out renames output actuator_trigger from env to actuator;
    /* CIF ENDTEXT */
    channel c
        from env to observer with trigger_out;
        from observer to env with trigger_out;
    endchannel;
    block observer;
        signalroute r
            from env to observer with trigger_out;
            from observer to env with trigger_out;
        connect c and r;
        /* CIF PROCESS (305, 91), (150, 75) */
        process observer;
            /* CIF TEXT (0, 57), (222, 140) */
            monitor st System_State;
            
            monitor event Observable_Event;
            
            successstates reached;
            /* CIF ENDTEXT */
            /* CIF START (459, 92), (70, 35) */
            START;
                /* CIF NEXTSTATE (459, 147), (70, 35) */
                NEXTSTATE Wait;
            /* CIF state (253, 0), (70, 35) */
            state reached;
            endstate;
            /* CIF state (678, 61), (70, 35) */
            state Wait;
                /* CIF input (663, 116), (90, 35) */
                input trigger_out;
                    /* CIF output (663, 171), (90, 35) */
                    output trigger_out;
                    /* CIF NEXTSTATE (673, 221), (70, 35) */
                    NEXTSTATE reached;
            endstate;
        endprocess observer;
    endblock;
endsystem;