/* CIF PROCESS (250, 150), (150, 75) */
process Controller;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF output (320, 65), (70, 35) */
        output ping;
        /* CIF NEXTSTATE (320, 115), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (664, 90), (70, 35) */
    state Wait;
        /* CIF input (664, 145), (70, 35) */
        input pong;
            /* CIF NEXTSTATE (664, 200), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Controller;