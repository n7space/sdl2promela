/* CIF PROCESS (250, 150), (150, 75) */
process Actuator;
    /* CIF START (523, 212), (80, 35) */
    START;
        /* CIF NEXTSTATE (528, 262), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (874, 239), (70, 35) */
    state Wait;
        /* CIF input (874, 294), (70, 35) */
        input ping;
            /* CIF output (874, 349), (70, 35) */
            output pong;
            /* CIF NEXTSTATE (874, 404), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Actuator;