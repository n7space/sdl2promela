/* CIF PROCESS (250, 150), (150, 75) */
process Actuator;
    /* CIF procedure (1094, 94), (88, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    procedure check_sync;
        /* CIF START (680, 135), (70, 35) */
        START;
            /* CIF output (672, 190), (84, 35) */
            output from_proc;
            /* CIF return (697, 240), (35, 35) */
            return ;
    endprocedure;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (765, 115), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Wait;
        /* CIF input (755, 170), (88, 35) */
        input check_sync;
            /* CIF output (755, 225), (88, 35) */
            output from_trans;
            /* CIF NEXTSTATE (764, 280), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Actuator;