/* CIF PROCESS (250, 149), (150, 75) */
process Actuator;
    /* CIF TEXT (394, 309), (267, 140) */
    -- Text area for declarations and comments
    
    dcl param MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (370, 55), (70, 35) */
    START;
        /* CIF NEXTSTATE (370, 105), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (977, 163), (70, 35) */
    state Wait;
        /* CIF input (962, 218), (94, 35) */
        input ping(param);
            /* CIF output (960, 273), (98, 35) */
            output pong(param);
            /* CIF NEXTSTATE (975, 328), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Actuator;