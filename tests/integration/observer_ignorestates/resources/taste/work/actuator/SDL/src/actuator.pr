/* CIF PROCESS (250, 150), (150, 75) */
process Actuator;
    /* CIF TEXT (394, 309), (267, 140) */
    -- Text area for declarations and comments
    
    dcl param MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (362, 49), (70, 35) */
    START;
        /* CIF NEXTSTATE (362, 99), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (977, 163), (70, 35) */
    state Wait;
        /* CIF input (963, 218), (94, 35) */
        input ping(param);
            /* CIF output (961, 273), (98, 35) */
            output pong(param);
            /* CIF NEXTSTATE (976, 328), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Actuator;