/* CIF PROCESS (250, 149), (150, 75) */
process Controller;
    /* CIF START (676, 244), (70, 35) */
    START;
        /* CIF output (676, 299), (70, 35) */
        output ping;
        /* CIF NEXTSTATE (676, 349), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (892, 228), (70, 35) */
    state Wait;
        /* CIF input (892, 283), (70, 35) */
        input pong;
            /* CIF output (892, 338), (70, 35) */
            output ping;
            /* CIF NEXTSTATE (892, 393), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Controller;