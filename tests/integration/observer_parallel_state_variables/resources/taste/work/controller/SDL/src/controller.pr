/* CIF PROCESS (250, 150), (150, 75) */
process Controller;
    /* CIF TEXT (379, 354), (267, 140) */
    -- Text area for declarations and comments
    
    dcl ping_param MyInteger;
    dcl pong_param MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (834, 304), (70, 35) */
    START;
        /* CIF task (810, 359), (116, 35) */
        task ping_param := 2;
        /* CIF output (805, 414), (126, 35) */
        output ping(ping_param);
        /* CIF NEXTSTATE (834, 464), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (1171, 315), (70, 35) */
    state Wait;
        /* CIF input (1138, 370), (135, 35) */
        input pong(pong_param);
            /* CIF NEXTSTATE (1171, 425), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Controller;