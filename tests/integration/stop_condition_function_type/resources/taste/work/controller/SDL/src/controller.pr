/* CIF PROCESS (250, 150), (150, 75) */
process Controller;
    /* CIF TEXT (948, 413), (267, 140) */
    -- Text area for declarations and comments
    
    dcl param MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (485, 200), (70, 35) */
    START;
        /* CIF task (474, 255), (91, 35) */
        task param := 23;
        /* CIF output (472, 310), (94, 35) */
        output ping(param);
        /* CIF NEXTSTATE (485, 360), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (700, 148), (70, 35) */
    state Wait;
    endstate;
endprocess Controller;