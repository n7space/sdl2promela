
--  Header file for function ComplexSdl in SDL language
--  Component type : FALSE
--  Instance of : 
--  Generated by TASTE on 2022-01-13 19:10:46
--  Context Parameters present : NO
--  Provided interfaces : impulse
--  Required interfaces : response
--  User-defined properties for this function:
--     * TASTE::Is_Component_Type = false
--     * TASTE_IV_Properties::FPGA_Configurations = AllModes
--  DO NOT EDIT THIS FILE, IT WILL BE OVERWRITTEN DURING THE BUILD
/* CIF Keep Specific Geode ASNFilename 'dataview-uniq.asn' */
use Datamodel;

system Complexsdl;

   /* CIF Keep Specific Geode PARAMNAMES x_in */
   signal impulse (MyInteger);

   /* CIF Keep Specific Geode PARAMNAMES x_in */
   signal response (MyInteger);

   channel c
      from env to Complexsdl with impulse;
      from Complexsdl to env with response;
   endchannel;

   block Complexsdl;

      signalroute r
      from env to Complexsdl with impulse;
      from Complexsdl to env with response;
      connect c and r;
         
   /* CIF PROCESS (250, 150), (150, 75) */
process Complexsdl;
    /* CIF TEXT (1202, 0), (267, 140) */
    -- Text area for declarations and comments
    
    DCL tmp MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (320, 21), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 71), (70, 35) */
        NEXTSTATE Wait;
    /* CIF label (770, 150), (70, 35) */
    connection jump:
        /* CIF output (748, 205), (112, 35) */
        output response(tmp);
        /* CIF NEXTSTATE (770, 255), (70, 35) */
        NEXTSTATE positive;
    /* CIF End Label */
    endconnection;
    /* CIF state (937, 80), (74, 40) */
    state negative;
        /* CIF input (924, 140), (101, 35) */
        input impulse(tmp);
            /* CIF decision (940, 195), (70, 50) */
            decision tmp;
                /* CIF ANSWER (895, 265), (70, 24) */
                (> 0):
                    /* CIF NEXTSTATE (895, 309), (70, 35) */
                    NEXTSTATE positive;
                /* CIF ANSWER (985, 265), (70, 24) */
                (0):
                    /* CIF NEXTSTATE (985, 309), (70, 35) */
                    NEXTSTATE positive;
            enddecision;
    endstate;
    /* CIF state (450, 21), (70, 35) */
    state Wait;
        /* CIF input (434, 76), (101, 35) */
        input impulse(tmp);
            /* CIF decision (450, 131), (70, 50) */
            decision tmp > 0;
                /* CIF ANSWER (405, 201), (70, 24) */
                (true):
                    /* CIF NEXTSTATE (405, 245), (70, 35) */
                    NEXTSTATE Positive;
                /* CIF ANSWER (495, 201), (70, 24) */
                (false):
                    /* CIF NEXTSTATE (491, 245), (76, 35) */
                    NEXTSTATE Negative;
            enddecision;
    endstate;
    /* CIF state (612, 24), (70, 35) */
    state Positive;
        /* CIF input (596, 79), (101, 35) */
        input impulse(tmp);
            /* CIF join (629, 134), (35, 35) */
            join jump;
    endstate;
endprocess Complexsdl;
endblock;
endsystem;
